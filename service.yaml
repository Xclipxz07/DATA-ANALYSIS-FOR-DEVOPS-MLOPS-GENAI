apiVersion: v1
kind: Service
metadata:
  name: sales-dashboard-service
spec:
  selector:
    app: sales-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
